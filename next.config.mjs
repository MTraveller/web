/** @type {import('next').NextConfig} */
const nextConfig = {
  // redirects: async () => [
  //   {
  //     source: '/app/:path*',
  //     destination: '/:path*',
  //     permanent: true,
  //   },
  // ],
};

export default nextConfig;
